---
- name: Install basic stuff
  community.general.pacman:
    name:
      - ansible
      - autoconf
      - base-devel
      - curl
      - git
      - gnupg
      - vim
      - wget
    state: present
