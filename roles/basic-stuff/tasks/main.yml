---
- name: Install basic stuff
  ansible.builtin.package:
    name:
      - ansible
      - vim
      - curl
      - wget
      - git
      - autoconf
      - build-essential
      - libncurses-dev
      - libssl-dev
    state: present
